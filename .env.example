# Copia a .env.local (Vite lo carga automaticamente). No subas .env.local a Git.
# Supabase → Project Settings → API

VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co

# Recomendado (docs Supabase): publishable
VITE_SUPABASE_PUBLISHABLE_KEY=

# Alternativa legada: anon (JWT eyJ...)
# VITE_SUPABASE_ANON_KEY=

# NUNCA en el frontend: secret / service_role.
